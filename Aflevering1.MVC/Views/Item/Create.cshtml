@model Aflevering1.MVC.Models.ItemAndPlacementData

@{
    ViewData["Title"] = "Create";
}

<h1>Tilføj</h1>

<h4>Vare</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Item.Name" class="control-label"></label>
                <input asp-for="Item.Name" class="form-control" />
                <span asp-validation-for="Item.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Item.Amount" class="control-label"></label>
                <input asp-for="Item.Amount" class="form-control" />
                <span asp-validation-for="Item.Amount" class="text-danger"></span>
                <label asp-for="Item.Unit" class="control-label"></label>
                <select asp-for="Item.Unit" class="form-control">
                    @foreach (var unit in Model.Units)
                    {
                        <option value="@unit.Name">@Html.DisplayFor(modelItem => unit.Name)</option>
                    }
                </select>
                <span asp-validation-for="Item.Unit" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Item.Placement" class="control-label"></label>
                <select asp-for="Item.Placement" class="form-control">
                    @foreach (var placement in Model.Placements)
                    {
                        <option value="@placement.Name">@Html.DisplayFor(modelItem => placement.Name)</option>
                    }
                </select>
                <span asp-validation-for="Item.Placement" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Tilbage til indkøbslisten</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
